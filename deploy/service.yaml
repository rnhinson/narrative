# service.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: narrative
  namespace: your-namespace          # match your OC_NAMESPACE
  labels:
    app: narrative
spec:
  selector:
    app: narrative
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
